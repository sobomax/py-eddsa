name: Build, Test & Publish Python

on:
  push:
  pull_request:
  release:
    types: [published]
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

permissions:
  contents: read

jobs:
  build_test_python:
    name: Build & Test Python
    runs-on: ${{ matrix.os }}-latest
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14", "3.15-dev"]
        compiler: [gcc, clang]
        include:
          - os: macos
            python-version: "3.11"
            compiler: clang
          - os: macos
            python-version: "3.12"
            compiler: clang
          - os: macos
            python-version: "3.13"
            compiler: clang
          - os: macos
            python-version: "3.14"
            compiler: clang
          - os: macos
            python-version: "3.15-dev"
            compiler: clang
          - os: windows
            python-version: "3.11"
            compiler: msvc
          - os: windows
            python-version: "3.12"
            compiler: msvc
          - os: windows
            python-version: "3.13"
            compiler: msvc
          - os: windows
            python-version: "3.14"
            compiler: msvc
          - os: windows
            python-version: "3.13"
            compiler: clang-cl
          - os: windows
            python-version: "3.14"
            compiler: clang-cl

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade build setuptools wheel
        shell: bash

      - name: Build package
        if: matrix.compiler != 'msvc' && matrix.compiler != 'clang-cl'
        run: CC=${{ matrix.compiler }} python -m build --sdist --wheel --outdir dist python
        shell: bash

      - name: Build package
        if: matrix.compiler == 'msvc' || matrix.compiler == 'clang-cl'
        run: python -m build --sdist --wheel --outdir dist python
        shell: bash

      - name: Install package
        run: python -m pip install dist/*.whl
        shell: bash

      - name: Run Python tests
        run: python -m unittest discover -s python/tests -p 'test_*.py'
        shell: bash

      - name: Run C tests
        if: matrix.os != 'windows'
        run: python python/setup.py runctest
        shell: bash

      - name: Run benchmark
        run: |
          WHEEL_FILE="$(ls dist/*.whl | head -n 1)"
          WHEEL_NAME="$(basename "${WHEEL_FILE}")"
          python python/tools/benchmark.py \
            --json "benchmark-python-${{ matrix.os }}-${{ matrix.compiler }}-${{ matrix.python-version }}.json" \
            --target "${{ matrix.os }}" \
            --compiler "${{ matrix.compiler }}" \
            --wheel "${WHEEL_NAME}"
        shell: bash

      - name: Run C benchmark
        if: matrix.compiler != 'msvc'
        run: |
          CC=${{ matrix.compiler }} python python/tools/benchmark_c.py \
            --json "benchmark-c-${{ matrix.os }}-${{ matrix.compiler }}-${{ matrix.python-version }}.json" \
            --target "${{ matrix.os }}" \
            --compiler "${{ matrix.compiler }}"
        shell: bash

      - name: Run C benchmark
        if: matrix.compiler == 'msvc'
        run: |
          python python/tools/benchmark_c.py \
            --json "benchmark-c-${{ matrix.os }}-${{ matrix.compiler }}-${{ matrix.python-version }}.json" \
            --target "${{ matrix.os }}" \
            --compiler "${{ matrix.compiler }}"
        shell: bash

      - name: Upload benchmark results
        uses: actions/upload-artifact@v7
        with:
          name: benchmark-${{ matrix.os }}-${{ matrix.compiler }}-${{ matrix.python-version }}
          path: benchmark-*.json

  benchmark_summary:
    name: Benchmark Summary
    needs: build_test_python
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Download benchmark results
        uses: actions/download-artifact@v8
        with:
          path: benchmark-results
          pattern: benchmark-*
          merge-multiple: true

      - name: Summarize benchmarks
        run: |
          python python/tools/benchmark_summary.py 'benchmark-results/*.json' --output benchmark-summary.md
          cat benchmark-summary.md >> "${GITHUB_STEP_SUMMARY}"
        shell: bash

  build_wheels:
    name: Build Wheels (${{ matrix.artifact-suffix }})
    needs: build_test_python
    runs-on: ${{ matrix.os }}-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu
            artifact-suffix: linux-x86_64
            cibw-archs-linux: x86_64
          - os: ubuntu
            artifact-suffix: linux-i686
            cibw-archs-linux: i686
          - os: ubuntu
            artifact-suffix: linux-aarch64
            cibw-archs-linux: aarch64
          - os: ubuntu
            artifact-suffix: linux-ppc64le
            cibw-archs-linux: ppc64le
          - os: ubuntu
            artifact-suffix: linux-s390x
            cibw-archs-linux: s390x
          - os: ubuntu
            artifact-suffix: linux-armv7l
            cibw-archs-linux: armv7l
          - os: ubuntu
            artifact-suffix: linux-riscv64
            cibw-archs-linux: riscv64
          - os: macos
            artifact-suffix: macos
          - os: windows
            artifact-suffix: windows
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Install cibuildwheel
        run: python -m pip install --upgrade cibuildwheel

      - name: Set up QEMU
        if: matrix.os == 'ubuntu' && matrix.cibw-archs-linux != 'x86_64' && matrix.cibw-archs-linux != 'i686'
        uses: docker/setup-qemu-action@v3
        with:
          platforms: all

      - name: Build Linux wheels
        if: matrix.os == 'ubuntu'
        run: python -m cibuildwheel python --output-dir dist
        env:
          CIBW_BUILD: "cp310-* cp311-* cp312-* cp313-* cp314-*"
          CIBW_ARCHS_LINUX: ${{ matrix.cibw-archs-linux }}
          CIBW_SKIP: "*-musllinux_*"
          CIBW_TEST_COMMAND: "python -c \"import eddsa; sec=bytes(range(32)); pub=eddsa.ed25519_genpub(sec); sig=eddsa.ed25519_sign(sec, pub, b'test'); assert eddsa.ed25519_verify(sig, pub, b'test')\""

      - name: Build wheels
        if: matrix.os != 'ubuntu'
        run: python -m cibuildwheel python --output-dir dist
        env:
          CIBW_BUILD: "cp310-* cp311-* cp312-* cp313-* cp314-*"
          CIBW_SKIP: "*-musllinux_*"
          CIBW_TEST_COMMAND: "python -c \"import eddsa; sec=bytes(range(32)); pub=eddsa.ed25519_genpub(sec); sig=eddsa.ed25519_sign(sec, pub, b'test'); assert eddsa.ed25519_verify(sig, pub, b'test')\""

      - name: Upload wheel artifacts
        uses: actions/upload-artifact@v7
        with:
          name: dist-${{ matrix.artifact-suffix }}
          path: dist/*.whl

  publish_all_wheels:
    name: Publish Python Distributions
    runs-on: ubuntu-latest
    needs: build_wheels
    environment:
      name: pypi
      url: https://pypi.org/p/py-eddsa
    permissions:
      actions: read
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v6

      - name: Download wheel artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist
          pattern: dist-*
          merge-multiple: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade build setuptools wheel

      - name: Build sdist
        run: python -m build --sdist --outdir dist python

      - name: Check release version number
        if: github.event_name == 'release' && github.event.action == 'published'
        run: |
          GIT_REF="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
          GIT_REF="${GIT_REF#refs/tags/}"
          python python/setup.py checkversion --tag="${GIT_REF}"

      - name: Publish package distributions to PyPI
        if: github.event_name == 'release' && github.event.action == 'published'
        uses: pypa/gh-action-pypi-publish@release/v1

  roll_release:
    name: Release
    needs: publish_all_wheels
    permissions:
      contents: write
    uses: sippy/cimagic/.github/workflows/RollReleaseDraft.yml@v3
